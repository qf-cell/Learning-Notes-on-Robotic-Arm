1.ROS2基础知识回顾
 （1）TF2是什么？/tf和/tf_static的区别是什么？
	 TF2用于管理和维护多个坐标系之间的空间关系
      /tf_static发布静态坐标变换，例如相机到机器人的坐标，采用“最后锁存”，即使是后加入的节点，也能收听到，节省带宽
      /tf发布动态坐标变换，用于高频坐标变换，例如机器人底盘对于地图的坐标，机械臂关节的相对运动
（2）Launch的作用？
	   launch的作用是自动化启动和配置多个ROS2节点，通过该文件，可以运行多个节点，设置参数，重命名话题/服务，管理节点间的依赖关系
	   通过定义DeclareLaunchArgument声明参数，通过LaunchConfiguration引用参数
	   在命令行启动时通过参数名:=数值启动
（3）XML的标签，属性，嵌套是什么？
	   标签：基本单位，用尖括号表示
	   属性：在起始标签内部定义的键值对
	   嵌套：一个元素包含在另一个元素内部
	   `<robot name="my_robot">`
	     `<link name="base_link">`
	  `</robot>`                                                                                                                                              
   (4) 机械臂的“旋转关节”和“平移关节”在物理上有什么区别？
	   